import React from "react";

interface RouteProps {
    children?: React.ReactNode;
    component?: React.ReactNode;
    path?: string;
}
export function Route(props:RouteProps):React.ReactNode|null{
    return null;
}
